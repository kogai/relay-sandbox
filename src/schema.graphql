type Node {
  name: String
  description: String
  isPublic: Boolean
}
type Gist {
  nodes: [Node]
}

type User {
  name: String!
  login: String!
  gists(last: Int!): Gist
}

type Query {
  # gists: [String]
  viewer: User
}
